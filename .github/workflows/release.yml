name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version tag (example: v1.2.3)"
        required: true
        type: string

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  publish:
    # Prevent duplicate releases when a workflow_dispatch run creates + pushes a tag.
    if: ${{ github.event_name != 'push' || github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      RELEASE_VERSION: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.version || github.ref_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Verify auth UI CSP freshness
        run: bash scripts/verify_auth_ui_csp.sh

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
          cache-dependency-path: |
            go.sum
            infra/cdk/go.sum

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Enable Corepack
        run: corepack enable

      - name: Install AWS CDK CLI
        run: npm install -g aws-cdk@2

      - name: Create and push tag (workflow_dispatch)
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: |
          set -euo pipefail

          if [[ "${GITHUB_REF_NAME}" != "main" ]]; then
            echo "workflow_dispatch releases must be run from the main branch (got '${GITHUB_REF_NAME}')" >&2
            exit 1
          fi
          if [[ "${RELEASE_VERSION}" != v* ]]; then
            echo "release version must start with 'v' (example: v1.0.0); got '${RELEASE_VERSION}'" >&2
            exit 1
          fi

          git fetch --tags --force
          if git rev-parse -q --verify "refs/tags/${RELEASE_VERSION}" >/dev/null; then
            existing_sha="$(git rev-list -n 1 "${RELEASE_VERSION}")"
            if [[ "${existing_sha}" != "${GITHUB_SHA}" ]]; then
              echo "tag '${RELEASE_VERSION}' already exists at ${existing_sha}; refusing to retag ${GITHUB_SHA}" >&2
              exit 1
            fi
            echo "tag '${RELEASE_VERSION}' already exists at ${existing_sha}"
            exit 0
          fi

          git tag "${RELEASE_VERSION}" "${GITHUB_SHA}"
          git push origin "refs/tags/${RELEASE_VERSION}"

      - name: Assert release target is on main
        run: |
          set -euo pipefail

          git fetch --no-tags origin "+refs/heads/main:refs/remotes/origin/main"
          git fetch --tags --force origin "refs/tags/${RELEASE_VERSION}:refs/tags/${RELEASE_VERSION}" || git fetch --tags --force origin

          tagged_sha="$(git rev-list -n 1 "${RELEASE_VERSION}")"
          if ! git merge-base --is-ancestor "${tagged_sha}" origin/main; then
            echo "release tag '${RELEASE_VERSION}' points at ${tagged_sha}, which is not an ancestor of origin/main" >&2
            exit 1
          fi

          echo "release tag '${RELEASE_VERSION}' points at ${tagged_sha}, an ancestor of origin/main"

      - name: Build release assets
        run: bash scripts/build_release_assets.sh "${RELEASE_VERSION}" dist/release

      - name: Verify artifact-driven deploy certification
        run: bash scripts/verify_artifact_deploy.sh dist/release

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          assets=(
            dist/release/lesser-linux-amd64
            dist/release/lesser-linux-arm64
            dist/release/lesser-darwin-amd64
            dist/release/lesser-darwin-arm64
            dist/release/lesser-lambda-bundle.tar.gz
            dist/release/lesser-lambda-bundle.json
            dist/release/lesser-auth-ui.tar.gz
            dist/release/lesser-deploy-assembly.tar.gz
            dist/release/lesser-deploy-assembly.json
            dist/release/checksums.txt
            dist/release/lesser-release.json
          )

          if gh release view "${RELEASE_VERSION}" >/dev/null 2>&1; then
            gh release upload "${RELEASE_VERSION}" "${assets[@]}" --clobber
          else
            gh release create "${RELEASE_VERSION}" "${assets[@]}" --verify-tag
          fi
