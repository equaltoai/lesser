This file is intentionally disabled.

The active PatternRepository adapter is currently implemented as `patternRepositoryAdapter` in
`pkg/moderation/advanced/pattern_repository_adapter.go` (used to bridge `pkg/storage/repositories.PatternRepository`
into the `pkg/moderation/advanced.PatternRepository` interface).

This file remains as historical context under the repo's non-destructive agent policy (no file deletes).
