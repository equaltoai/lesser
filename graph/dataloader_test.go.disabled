This file is intentionally disabled.

DataLoader behavior is covered by `graph/dataloader_round12_test.go`.

The repository enforces a non-destructive policy for agents (no file deletes), so this file remains as
historical context. It can be removed manually once that policy is lifted.
